#include <stdlib.h>
#include <stdio.h>


void build_matrix(int size, *matrix))
/*
    Builds an adjacency matrix for a square directed graph.  See Cormen et al ch. 22.
 */
{
    int node = 1;

    for ( x_node = 1; node <= size * size; node++ ) {
        for ( y_node = 1; node <= size * size; node++ ) {
            if ( 
        }
    }

}
